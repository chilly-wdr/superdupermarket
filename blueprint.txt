Supermarket_Management_System
├── main.py
├── database.py
├── product.py
├── customer.py
├── rewards.py
├── reports.py
├── sql
│   └── supermarket.sql
├── README.txt

main - menus and funcs
database - python + sql

whenever a bill is done..the stock has to go down?.. - make a separate transaction table 
update reward points automatically..how.. - PYTHON..calculate from amount col
redeem points..change total? - python again first decrease and then add earned points
update stock number - use the transaction table
how add items.. - cashier has to enter the items into python 'cart' (a tuple of list/list of tuple)

stock:
 product_id(primary) | product_name(not null + unique) | stock_available(not null) | last_restock_date(not null) | market_price(not null) | price (not null) | 
   add new product
   change product_name
   update stock / restock (update no of stock and r_date)
   delete product
   add new price 
   change market price (use p_id)
   calculate profit (price - mp)  


customer/rewards:
 customer_id(primary) | customer_name(not null) | mobile_number(not null + unique) | DOJ | reward_points(default=0)
   add new customer
   update customer_name
   update mobile_number
   view reward points (use mobile number)
   
   
billing:
 bill_id(primary) | date_of_transaction(not null) | time_of_transaction(not null) | staff_id(foreign) | no_of_items(not null) | total_amount(not null) | reward_customer(Y/N) | customer_id(foreign) | issued_reward_points | reward_points_redeemed(default=0)
   new bill
   view bills
   

transaction: (to update stock number) 
 bill_id(foreign) | product_id(foreign) | qty | unit_price | total_price 
   cart->transaction

staff:
 staff_id(primary) | staff_name | mobile_number
   add new staff
   update name
   update number
   delete staff details


discount:
 discount_id(primary) | product_id(foreign) | discount_percentage | discounted_price | start_date | end_date | rewards_only
   add new discount 
   